/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { TogglePipe } from './toggle.pipe';

describe('Pipe: Toggle', () => {
  it('create an instance', () => {
    let pipe = new TogglePipe();
    expect(pipe).toBeTruthy();
  });
});
